const asyncHandler = require('express-async-handler')

const {{properCase service}}Service = require('@services/{{service}}')

module.exports = router => {
  router.{{method}}(
    '/{{path}}',
    [],
    asyncHandler(async (req, res) => {
      res.status(200).json({})
    })
  )
}
